
function(a,b){if(a===b)return 0;}
function sourceEmpty(source, target) {   if (source.length === 0 && target.length === 0) {     return true;   }   return false; }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     return false;   }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {         return true;     }     return false; }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {       return true;     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (source[i] === target[i]) {         return true;       }     }     return false;   }
function sourceEmpty(source, target) {   if (source.length === 0 && target.length === 0) {     return true;   }   if (source === target) {     return true;   }   return false; }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {         return true;     }     for (var i = 0; i < source.length; i++) {         if (source[i] === target[i]) {             return true;         }     }     return false; }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (let i = 0; i < source.length; i++) {       if (source[i] === target[i]) {         return true;       }     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (!empty(source[i])) {         return true;       }     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (source[i] !== target[i]) {         return true;       }     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (!source[i]) {         return true;       }     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (let i = 0; i < source.length; i++) {       if (source[i] !== target[i]) {         return true;       }     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (source[i] !== target[i]) {         return false;       }     }     return true;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (!source[i]) {         return false;       }     }     return true;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (let i = 0; i < source.length; i++) {       if (source[i] !== target[i]) {         return false;       }     }     return true;   }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {         return true;     }     for (var i = 0; i < source.length; i++) {         if (source[i] !== target[i]) {             return true;         }     }     return false; }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {         return true;     }     for (var i = 0; i < source.length; i++) {         if (source[i] !== target[i]) {             return false;         }     }     return true; }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     for (var i = 0; i < source.length; i++) {       if (source[i] === undefined) {         return true;       }     }     return false;   }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {         return true;     }     for (var i = 0; i < source.length; i++) {         if (!source[i]) {             return true;         }     }     return false; }
function sourceEmpty(source, target) {     if (source.length === 0 && target.length === 0) {         return true;     }     for (var i = 0; i < source.length; i++) {         if (!source[i]) {             return false;         }     }     return true; }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     if (source.length === 0 && target.length === 0) {       return false;     }     if (source.length === 1 && target.length === 1) {       return true;     }     return false;   }
function sourceEmpty() {     if (source.length === 0 && target.length === 0) {       return true;     }     if (source.length === 0 && target.length === 0) {       return false;     }     if (source.length === 1 && target.length === 0) {       return true;     }     return false;   }
function sourceEmpty(source, target) {   if (source.length === 0 && target.length === 0) {     return true;   } else if (source.length === 0 && target.length === 0) {     return false;   } else if (source.length === 1 && target.length === 1) {     return true;   }   return false; }
