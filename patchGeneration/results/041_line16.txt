
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substr(0, line.indexOf("c")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("b") > -1) { 			line = line.substring(0, line.indexOf("b")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		console.log(line); 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("e") > -1) { 			line = line.substring(0, line.indexOf("e")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("x") > -1) { 			line = line.substring(0, line.indexOf("x")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("d") > -1) { 			line = line.substring(0, line.indexOf("d")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("A") > -1) { 			line = line.substring(0, line.indexOf("A")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("ab") > -1) { 			line = line.substring(0, line.indexOf("ab")); 		} 		return line; 	}
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("b") > -1) { 			line = line.substring(0, line.indexOf("b")); 		} 		if (line.indexOf("c")
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("b") > -1) { 			line = line.substring(0, line.indexOf("b")); 		} 		if (line.indexOf("d")
function(hljs) {   var NUMBER = {     className: 'number',     begin: '\'', end: '\'',     relevance: 0   };   var STRING = {     className: 'string',     begin: '\'', end: '\'',     relevance: 0   };   var CLASS_NUMBER = {     className: 'class',     begin: '\'', end: '\'',     relevance: 0   };   var CLASS_STRING = {     className: 'class',     begin: '\'', end: '\'',
function(hljs) {   var NUMBER = {     className: 'number',     begin: '\'', end: '\'',     relevance: 0   };   var STRING = {     className: 'string',     begin: '\'', end: '\'',     relevance: 0   };   var NUMBER_START = {     className: 'number',     begin: '\'', end: '\'',     relevance: 0   };   var NUMBER_END = {     className: 'number',     begin: '\'', end: '\'',
function(hljs) {   var NUMBER = {     className: 'number',     begin: '\'', end: '\'',     relevance: 0   };   var STRING = {     className: 'string',     begin: '\'', end: '\'',     relevance: 0   };   var NUMBER_START = {     className: 'number',     begin: '\'', end: '\'',     relevance: 0   };   var STRING_END = {     className: 'string',     begin: '\'', end: '\'',
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("b") > -1) { 			line = line.substring(0, line.indexOf("b")); 		} 		if (line.indexOf("b")
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("b") > -1) { 			line = line.substring(0, line.indexOf("b")); 		} 		if (line.indexOf("a")
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a")
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("d") > -1) { 			line = line.substring(0, line.indexOf("d")); 		} 		if (line.indexOf("b")
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("d") > -1) { 			line = line.substring(0, line.indexOf("d")); 		} 		if (line.indexOf("e")
function(line) { 		if (line.indexOf("c") > -1) { 			line = line.substring(0, line.indexOf("c")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("a") > -1) { 			line = line.substring(0, line.indexOf("a")); 		} 		if (line.indexOf("a")
function(hljs) {   var NUMBER = {     className: 'number',     begin: '\'', end: '\'',     relevance: 0   };   var STRING = {     className: 'string',     begin: '\'', end: '\'',     relevance: 0   };   var NUMBER_OR_STRING = {     className: 'string',     begin: '\'', end: '\'',     relevance: 0   };   var STRING_OR_NUMBER = {     className: 'string',     begin: '\'', end:
