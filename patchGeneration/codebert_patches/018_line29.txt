int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0; //
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0;
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0; /*
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0;//
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0;
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0; ///
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0;*/
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0; }
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0; /**
int length = !prefix_lengths.isEmpty() ? Collections.max(prefix_lengths) : 0;***
